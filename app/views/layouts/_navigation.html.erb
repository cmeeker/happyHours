<div class = "login">
	<% if current_user %>Logged in as <%= current_user.username %> | 
	<%= link_to 'Log out', auth_path, method: 'destroy' %>
	<% else %>
		<%= link_to 'Log in', new_auth_path %>
	<% end %>
</div>